This session contains:
======================

Makefile			Makefile with all the necessaries targets
constants.h			Constants of the program
pi_seq.c			Pi Computation 
pi_omp.c			OMP version Pi Computation 
pi_mpi.c			MPI version Pi Computation 
experiment-mpi.sh		script to run mpi codes. Run it for more information
experiment-omp.sh		script to run omp codes. Run it for more information
jgraph				executable used to create the figures
grafica-mpi/omp.jgr		jgraph figure source code (templates)
